<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8 text-xl">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Setting</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" />
</head>
<body class="bg-yellow-100">
    <%- include('_navbar') %>
    <%- include('_sidebar') %>

    <div class="grid place-items-center bg-white m-10 rounded-3xl shadow-lg shadow-white w-1/2 mx-auto p-8">
        <form action="/profile/privacy/<%= profile.id %>/update" method="POST" class="text-center">
            <div class="mb-4">
                <label for="firstName" class="block text-sm font-medium text-gray-700">Username</label>
                <input type="text" id="firstName" name="username" class="mt-1 p-2 block w-full border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-yellow-500 focus:border-transparent" required value="<%= profile.username %>">
            </div>
            <div class="mb-4">
                <label for="lastName" class="block text-sm font-medium text-gray-700">Email</label>
                <input type="text" id="lastName" name="email" class="mt-1 p-2 block w-full border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-yellow-500 focus:border-transparent" required value="<%= profile.email %>">
            </div>
            <div class="mb-4">
                <label for="address" class="block text-sm font-medium text-gray-700">Password</label>
                <input id="address" name="password" rows="3" class="mt-1 p-2 block w-full border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-yellow-500 focus:border-transparent" required>
            </div>
            <div class="mt-4">
                <button type="submit" class="bg-yellow-500 text-white py-2 px-4 rounded-md hover:bg-yellow-600 focus:outline-none focus:bg-yellow-600">Save Changes</button>
            </div>
        </form>
    </div>
    
    <div class="grid grid-cols-1 gap 1 bg-white m-10 rounded-3xl shadow-lg shadow shadow-white w-2/6 text-center items-center">
            <a href="/profile/setting/<%= profile.Profile.id %>" class="text-black font-bold hover:bg-yellow-100 p-8 text-xl">Manage Your Account</a>
            <a href="/profile/privacy/<%= profile.Profile.id %>" class="text-black font-bold hover:bg-yellow-100 p-8 text-xl">Privacy</a>
    </div>
</body>
</html>