<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" />
</head>
<body>
      
        <div class="flex items-center justify-center h-screen">
        <div class="bg-black p-8 rounded-lg shadow-lg w-full max-w-md">
            <h1 class="text-3xl font-bold text-white text-center mb-6">Log in to Tikitaka</h1>
            <% if(msg) { %>
                <h1 class="text-white"><%= msg %></h1>
            <% } %>
            <div class="bg-white p-6 rounded-lg shadow-md">
                <form method="post" action="/users/login">
                    <div class="mb-4">
                        <label for="inputEmail" class="block text-gray-700">Email</label>
                        <input type="email" class="form-input border mt-1 block w-full rounded-md border-gray-300" id="inputEmail" name="email" required>
                    </div>
                    <div class="mb-4">
                        <label for="inputPassword" class="block text-gray-700">Password</label>
                        <input type="password" class="form-input border mt-1 block w-full rounded-md border-gray-300" id="inputPassword" name="password">
                    </div>
                    <button type="submit" class="bg-black text-white px-4 py-2 rounded-md hover:bg-white hover:text-black">Login</button>
                </form>
                <div class="mt-4 text-center">
                    <p class="text-gray-600">Don't have an account? <a href="/users/register" class="text-black hover:text-blue-700">Sign Up</a></p>
                </div>
            </div>
        </div>
    </div>
    
   
</body>
</html>